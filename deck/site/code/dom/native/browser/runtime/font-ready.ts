// Superseded by ./fontset.ts. A `<global:X>` tag is emitted verbatim as the binding identifier, so the runtime had
// to be renamed to a single hyphen-free word (`fontset`) to produce valid JS. This file is intentionally empty;
// nothing docks `<global:font-ready>` anymore. Safe to delete.
export {}
