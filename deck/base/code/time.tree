
# Time: the current epoch time. Implementation selected at build time, hidden behind this API.

load @term/base/code/native/time
  find epoch-millis

task now
  like number
  send back
    call epoch-millis
