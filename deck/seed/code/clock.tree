
# Clock: current time and delays. Platform implementation is selected at build time and hidden behind this API.

load @term/seed/code/native/{platform}/clock
  find current-time
  find delay

task now
  like number
  send back
    call current-time

task sleep
  note async
  take ms, like number
  send back
    call delay
      read ms
      wait true
