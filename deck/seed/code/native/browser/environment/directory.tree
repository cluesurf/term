
# The working directory. Forwards to the linked directory runtime shim, which uses the URL path. Internal: reached
# only through the public environment API.

dock load
  load <global:directory>, name directory

task get
  like text

  send back
    call directory/get

task set
  take path, like text

  call directory/set
    read path
