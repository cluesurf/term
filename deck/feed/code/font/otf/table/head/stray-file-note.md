Created by mistake mid-edit (a stray placeholder file, never given real content) and renamed out of the way
rather than left as `mine2.tree`, which would have looked like a second real spec file. Nothing to see here.
