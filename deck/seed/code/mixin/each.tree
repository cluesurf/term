
# Compile-time mixin: map a template over an enumeration, generating its body for each item.

tree define-each
  take list
  take tree-name

  hook fuse
    walk list, read list
      hook next
        take item
        fuse read tree-name
          read item
