
# CLI command in the dock DSL. A `make` command with options and a handler.
# Arg-policy resolution is ours; argv tokenizing delegates to native per backend.

dock make
  take name
    like text
    need true

  take title
    like text

  call make-deck
    bind name, read name
    bind title, read title
