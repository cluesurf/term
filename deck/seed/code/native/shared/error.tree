# Shelved 2026-08-28. This module never built: it loads `@term/seed/code/base/result` and `base/kink`, paths that do
# not exist, so `result` was unresolved and the `note unsafe` body was dropped as an annotation. Now that `note unsafe`
# is a real guard, the checker reads it and the mismatch shows. The design that replaces it is
# note/term/hive/03-exception.md (a form that is `like exception`, `halt <form>`, `halt take`).
note draft

load @term/seed/code/result
  find result

load @term/seed/code/kink
  find kink

task make
  take code, like text
  take message, like text
  take fields, like hash, need false
  take cause, like kink, need false
  take trace, like boolean, fall false
  send back
    make kink
      bind code, read code
      bind note, read message
      bind hint, read fields

task throw
  take error, like kink
  halt kink, read error

task catch
  take work, like task
  note unsafe
    save value
      call work
    send back
      make okay
        bind value, read value
  halt take
    take error
    send back
      make error
        bind value, read error

task wrap
  take data
  send back
    make okay
      bind value, read data

task fail
  take error, like kink
  send back
    make error
      bind value, read error
