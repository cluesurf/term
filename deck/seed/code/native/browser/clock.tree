
# The clock. Forwards to the linked clock runtime shim. Internal: reached only through the public clock API.

dock load
  load <global:clock>, name clock

load ./clock/now
  find get

task delay
  note async
  take duration, like size

  call clock/delay
    wait true
    read duration

task now
  send back
    call get
