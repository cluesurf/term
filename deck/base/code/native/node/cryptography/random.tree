# Node.js secure random implementation (the host node:crypto module). randomBytes returns a Buffer, which is the raw bytes currency. Internal: reached only through the public random API.

dock load
  load <node:crypto>, name crypto

task random-bytes
  take size, like number
  like bytes
  send back
    call crypto/random-bytes
      read size
