// GENERATED by task/make-grammar.mjs from deck/mill/code/deck — never edit. Run `pnpm run make:grammar`.
// The deck-manifest and lockfile grammars, inlined so the published package carries them.

export const DECK_GRAMMAR = "# The deck manifest grammar, written against the REAL deck.tree files of this tree and the manifest reader's\n# whole field surface (deck/deck/test/manifest.test.ts): the name is a path word (`deck @term/seed`), the fields\n# come in any order, and an unknown field passes over with `mine node` the way the hand-rolled reader ignores it.\n# The executor (compile/mill-run.ts) reads a manifest through this; test/compile/mill-run.ts holds it against\n# deck/deck/code/manifest.ts on every manifest in the tree, and deck/deck/code/manifest.ts itself reads through\n# this grammar (mill-self-hosting-0005).\n\n\n\nmine deck\n  mine list\n    mine any\n      mine form, like deck-def\n        site deck\n      mine form, like role\n        site role\n      mine form, like load\n        site load\n      mine node\n\nmine deck-def\n  mine term, term deck\n    mine path\n      site name\n    mine list\n      mine any\n        mine form, like deck-head\n          site head\n        mine form, like deck-code\n          site code\n        mine form, like deck-mark\n          site mark\n        mine form, like deck-lock\n          site lock\n        mine form, like deck-link\n          site link\n        mine form, like deck-host\n          site host\n        mine form, like deck-case\n          site case\n        mine form, like deck-mind\n          site mind\n        mine form, like deck-hook\n          site hook\n        mine form, like deck-bear\n          site bear\n        mine form, like deck-boot\n          site boot\n        mine form, like deck-tool\n          site tool\n        mine form, like deck-test\n          site test\n        mine form, like deck-line\n          site line\n        mine form, like deck-book\n          site book\n        mine form, like deck-call\n          site call\n        mine form, like deck-task\n          site task\n        mine form, like deck-role\n          site role\n        mine form, like deck-deck\n          site deck\n        mine form, like deck-hide\n          site hide\n        mine form, like deck-site\n          site site\n        mine form, like deck-view\n          site view\n        mine form, like deck-make\n          site make\n        mine form, like deck-term\n          site term\n        mine form, like deck-text\n          site text\n        mine form, like deck-cite\n          site cite\n        mine form, like deck-sort\n          site sort\n        mine node\n\n# `head <one line about the package>`\nmine deck-head\n  mine term, term head\n    mine text\n      site text\n\n# `code <0.0.16>`: the version\nmine deck-code\n  mine term, term code\n    mine text\n      site text\n\n# `mark <0.0.1>`: the version in its older spelling, or `mark private`\nmine deck-mark\n  mine term, term mark\n    mine any\n      mine text\n        site text\n      mine term\n        site term\n\n# `lock mit` or `lock <Apache 2.0>`\nmine deck-lock\n  mine term, term lock\n    mine any\n      mine text\n        site text\n      mine term\n        site term\n\n# `link @term/seed, code <0.0.x>` (or the older `mark <0.0.x>`), with an optional `have <n>`\nmine deck-link\n  mine term, term link\n    mine path\n      site name\n    mine maybe\n      mine any\n        mine term, term code\n          mine text\n            site code\n        mine term, term mark\n          mine text\n            site code\n    mine maybe\n      mine term, term have\n        mine any\n          mine text\n            site have\n          mine code\n            site have\n\n# `case work` holds the DEV dependencies: `link` forms and `host` groups\nmine deck-case\n  mine term, term case\n    mine term\n      site name\n    mine list\n      mine any\n        mine form, like deck-link\n          site link\n        mine form, like deck-host\n          site host\n\n# `host <registry url>` with child links: dependencies resolved at that registry\nmine deck-host\n  mine term, term host\n    mine text\n      site registry\n    mine list\n      mine form, like deck-link\n        site link\n\n# `mind <Lance Pollard>, base <lp@elk.fm>` with an optional child `site <...>`\nmine deck-mind\n  mine term, term mind\n    mine text\n      site name\n    mine maybe\n      mine term, term base\n        mine text\n          site base\n    mine maybe\n      mine term, term site\n        mine text\n          site site\n\n# `hook build, task ./task/build`\nmine deck-hook\n  mine term, term hook\n    mine term\n      site name\n    mine term, term task\n      mine path\n        site path\n\n# `bear ./code`: what the package exports\nmine deck-bear\n  mine term, term bear\n    mine path\n      site path\n\n# `boot ./code/boot`: the app entry, what `term wake` scaffolds and `term boot` runs\nmine deck-boot\n  mine term, term boot\n    mine path\n      site path\n\n# `tool ./tool`: the tools directory\nmine deck-tool\n  mine term, term tool\n    mine path\n      site path\n\n# `test ./test`\nmine deck-test\n  mine term, term test\n    mine path\n      site path\n\n# `line ./code/line`: the CLI entry\nmine deck-line\n  mine term, term line\n    mine path\n      site path\n\n# `book ./book`: the docs\nmine deck-book\n  mine term, term book\n    mine path\n      site path\n\n# `call ./call`\nmine deck-call\n  mine term, term call\n    mine path\n      site path\n\n# `task ./task`\nmine deck-task\n  mine term, term task\n    mine path\n      site path\n\n# `role ./base/role`: the role file\nmine deck-role\n  mine term, term role\n    mine path\n      site path\n\n# `deck ./deck/load`: a sub-package entry\nmine deck-deck\n  mine term, term deck\n    mine path\n      site path\n\n# `hide true`\nmine deck-hide\n  mine term, term hide\n    mine term\n      site flag\n\n# `site <https://...>`\nmine deck-site\n  mine term, term site\n    mine any\n      mine text\n        site text\n      mine path\n        site text\n\n# `view ./view/tree.gif`\nmine deck-view\n  mine term, term view\n    mine any\n      mine text\n        site text\n      mine path\n        site text\n\n# `make <security>`: a keyword, repeatable\nmine deck-make\n  mine term, term make\n    mine text\n      site text\n\n# `term <Apache 2.0>` or `term tree-code`: a licence or keyword term\nmine deck-term\n  mine term, term term\n    mine any\n      mine text\n        site text\n      mine term\n        site text\n\n# `text <The Term Secret Access Library>`: the long title\nmine deck-text\n  mine term, term text\n    mine text\n      site text\n\n# `cite <Lance Pollard>, base <lp@elk.fm>`\nmine deck-cite\n  mine term, term cite\n    mine text\n      site name\n    mine maybe\n      mine term, term base\n        mine text\n          site base\n\n# `sort library`\nmine deck-sort\n  mine term, term sort\n    mine any\n      mine text\n        site text\n      mine term\n        site term\n\n\nmine role\n  mine term, term role\n    mine term\n      site name\n    mine list\n      mine form, like role-take\n        site take\n\nmine role-take\n  mine term, term take\n    mine path\n      site glob\n    mine list\n      mine form, like role-miss\n        site miss\n\nmine role-miss\n  mine term, term miss\n    mine path\n      site glob\n\n\n\nmine load\n  mine term, term load\n    mine text\n      site path\n    mine maybe\n      mine term, term name\n        mine term\n          site name\n    mine maybe\n      mine term, term host\n        site host\n    mine list\n      mine form, like find\n        site find\n    mine list\n      mine form, like load\n        site load\n\nmine find\n  mine term, term find\n    mine term\n      site text\n    mine maybe\n      mine term, term name\n        mine term\n          site name\n"

export const LOCK_GRAMMAR = "# The lockfile grammar: `lock <version>` then one `deck` entry per resolved package, each with its pinned\n# code, its content hash, its store site and its links. Written by deck/deck/code/lock.ts (writeLockfile),\n# read back through the executor; test/compile/mill-run.ts holds the two against each other.\n\nmine lock-file\n  mine list\n    mine any\n      mine form, like lock-head\n        site head\n      mine form, like lock-deck\n        site deck\n\nmine lock-head\n  mine term, term lock\n    mine text\n      site version\n\nmine lock-deck\n  mine term, term deck\n    mine path\n      site name\n    mine list\n      mine any\n        mine form, like lock-code\n          site code\n        mine form, like lock-hash\n          site hash\n        mine form, like lock-site\n          site site\n        mine form, like lock-link\n          site link\n\nmine lock-code\n  mine term, term code\n    mine text\n      site text\n\nmine lock-hash\n  mine term, term hash\n    mine text\n      site text\n\nmine lock-site\n  mine term, term site\n    mine text\n      site text\n\nmine lock-link\n  mine term, term link\n    mine path\n      site name\n    mine maybe\n      mine term, term code\n        mine text\n          site code\n"
