# UUID generation. The generic interface; each public verb delegates to the per-backend native implementation, which lives in its own file (`native/<env>/uuid`). Input and output stay in the currency types.

load @term/base/code/native/uuid
  find generate-v4

task version4
  like text
  send back
    call generate-v4
