
# Client route tree in the route DSL. Maps paths to zone components.
# Route matching is ours; navigation delegates to the browser History API.

hook /
  view home-page

hook /counter
  view counter
    bind label, text <hits>

hook /users/:id
  take path
    take id
      like text
  view user-detail
    bind id, read id
