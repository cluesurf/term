# rust secure random implementation. Forwards to the linked crypto runtime shim (the OS-backed generator, returning raw bytes). Internal: reached only through the public random API.

dock load
  load <global:crypto>, name crypto

task random-bytes
  take size, like number
  like bytes
  send back
    call crypto/random-bytes
      read size
