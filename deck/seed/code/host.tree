# SHELVED 2026-08-29, out of the build. A design sketch in pre-current syntax that nothing loads; the types it
# names were never declared (the unknown-type warning counts them). Delete `note draft` to bring it back.
note draft

form host
  link mark, like text
  link name, like text
  link term, like term
  link head, like text
  link read, like text
  link deck, hash deck, link name
  link link, list face-link
  link face, list face
  link cite, list cite
  link site, like site