note <Tests for the deque (double-ended queue). Push and pop at both ends; pops return maybe, unwrapped with a fallback.>

load @term/base/code/list/deque
  find deque

load @term/base/code/list
  find list

load @term/base/code/maybe
  find unwrap-or

test <push-back then pop-front gives FIFO order>
  save d
    make deque
      bind items
        make list
  call push-back
    read d
    code 1
  call push-back
    read d
    code 2
  want hold
    call is-equal
      call unwrap-or
        call pop-front
          read d
        code 0
      code 1

test <push-front then pop-front gives LIFO order>
  save d
    make deque
      bind items
        make list
  call push-front
    read d
    code 1
  call push-front
    read d
    code 2
  want hold
    call is-equal
      call unwrap-or
        call pop-front
          read d
        code 0
      code 2

test <pop-back removes from the back>
  save d
    make deque
      bind items
        make list
  call push-back
    read d
    code 1
  call push-back
    read d
    code 2
  want hold
    call is-equal
      call unwrap-or
        call pop-back
          read d
        code 0
      code 2

test <count reflects the number of items>
  save d
    make deque
      bind items
        make list
  call push-back
    read d
    code 7
  call push-front
    read d
    code 8
  want hold
    call is-equal
      call count
        read d
      code 2

test <a fresh deque is empty>
  save d
    make deque
      bind items
        make list
  want hold
    call is-empty
      read d

test <pop-front on an empty deque is none>
  save d
    make deque
      bind items
        make list
  want hold
    call is-equal
      call unwrap-or
        call pop-front
          read d
        code 99
      code 99
